/* eslint-disable no-unused-vars */
export function CardTest(props) {
  const { id, name, price } = props;

  return (
    <div>
      <p>{id}</p>
      {/* <p>{name}</p> */}
      {/* <p>{price}</p> */}
    </div>
  );
}
