{
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "prepare": "husky install",
    "start": "concurrently \"npm i && npm start --prefix api\" \"npm i && npm run dev --prefix react-app\""
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
