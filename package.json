{
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "prepare": "husky install",
    "start": "concurrently \"npm start --prefix api\" \"npm run dev --prefix react-app\""
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "js-cookie": "^3.0.5"
  }
}
